# Spring properties
spring:
  application:
     name: app4
  freemarker:
    enabled: false           
  thymeleaf:
    cache: false             
    prefix: classpath:/client4/templates/
                             

# Map the error path to error template (for Thymeleaf)
error:
  path: /error

# HTTP Server
server:
  port: 2070   # HTTP (Tomcat) port

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2030/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 5
      

